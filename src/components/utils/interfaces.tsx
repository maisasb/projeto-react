export interface Filter {
    [key:string]: boolean
}

/*acordao?: boolean,
    comunicado?: boolean,
    contribuinte?: boolean,
    decreto?: boolean,
    normativa?: boolean,
    lei?: boolean,
    tributaria?: boolean,
    portaria?: boolean,
    resolucao?: boolean
    */